#include <stdio.h>

int main() {
    int t, cities, pilots, a, b, i, j;
    scanf("%d", &t);

    for (i = 0; i < t; ++i) {
        scanf("%d%d", &cities, &pilots);

        for (j = 0; j < pilots; ++j) {
            scanf("%d%d", &a, &b);
        }
    }

    return 0;
}
